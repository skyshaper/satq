language: ruby
rvm:
  - 1.9.3
before_script:
  - psql -c 'create database satq_test;' -U postgres
script: 
  - RAILS_ENV=test bundle exec rake --trace db:migrate
  - bundle exec rake db:test:prepare
  - bundle exec rake test