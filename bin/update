#!/bin/sh -eux
bundle update
bundle update rails # Workaround, otherwise bundler will not update rails?!
if git diff --quiet --exit-code; then
    exit 0
fi
./bin/rake test
git add -A
git commit -m "Update dependencies"
git push
git push deploy master